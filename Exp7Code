#include <iostream>
#include <cstring>
using namespace std;

class String
{
    char str[100];

public:
    String()
    {
        str[0] = '\0';
    }

    String(const char s[])
    {
        strcpy(str, s);
    }

    void accept()
    {
        cout << "Enter a string: ";
        cin.getline(str, 100);
    }

    void display()
    {
        cout << "String is: " << str << endl;
    }

    ~String()
    {
        cout << "Destructor called." << endl;
    }
};

int main()
{
    String s1;

    s1.accept();
    s1.display();

    return 0;
}
